{% extends 'base.html.twig' %}

{% block title %}Категория{% endblock %}

{% block body %}
    <h3>Категория</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ category.type }}</td>
            </tr>
        </tbody>
    </table>

    <a  class="btn btn-primary" href="{{ path('app_info_category_index') }}">Back to list</a>

    <a  class="btn btn-primary" href="{{ path('app_info_category_edit', {'id': category.id}) }}">Edit</a>

    {{ include('info_category/_delete_form.html.twig') }}
{% endblock %}
