{% extends 'base.html.twig' %}

{% block title %}Клиент{% endblock %}

{% block body %}
    <h3>Клиент</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ client.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ client.price }}</td>
            </tr>
            <tr>
                <th>Scuare</th>
                <td>{{ client.scuare }}</td>
            </tr>
            <tr>
                <th>Pronoun</th>
                <td>{{ client.pronoun }}</td>
            </tr>
        </tbody>
    </table>

    

    <a class="btn btn-primary" href="{{ path('app_info_arenda_edit', {'id': client.id}) }}">Edit</a>

    {{ include('info_arenda/_delete_form.html.twig') }}
	
	<a class="btn btn-primary" href="{{ path('app_info_arenda_index') }}">Back to list</a>
{% endblock %}
